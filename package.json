{
  "name": "linkedin-scraping",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-watch move",
    "start-popup": "cd src/popup-react && npm run watch",
    "start-content": "cd src/content-react && npm run watch",
    "start-options": "cd src/options-react && npm run watch",
    "move": "cp -r ./public/ ./build",
    "watch-public": "npm-watch move",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-watch": "^0.11.0"
  },
  "watch": {
    "move": {
      "patterns": [
        "public/*"
      ],
      "ignore": "node_modules"
    }
  }
}
